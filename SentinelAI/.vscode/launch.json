{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Launch Chrome against Flask",
            "url": "http://localhost:5000",
            "webRoot": "${workspaceFolder}/frontend/src"
        }
    ]
}